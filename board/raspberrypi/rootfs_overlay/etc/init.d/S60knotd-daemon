#!/bin/sh
#
# Start KNoTd
#

DAEMON=/etc/knot/knotd_respawn

start() {
        printf "Starting KNoTd: "
        $DAEMON &
        echo $! > /tmp/knotd.pid
        echo "OK"
}

stop() {
        printf "Stopping KNoTd: "
        kill `cat /tmp/knotd.pid`
        echo "OK"
}

restart() {
        stop
        start
}


case "$1" in
  start)
	start
	;;
  stop)
	stop
	;;
  restart|reload)
	restart
	;;
  *)
	echo "Usage: $0 {start|stop|restart}"
	exit 1
esac

exit $?
